# ComfyUI-MOVA Requirements
# Note: Most dependencies should already be installed with ComfyUI
# MOVA package must be installed in the same environment (e.g. test Docker):
#   pip install git+https://github.com/OpenMOSS/MOVA.git
# Or from local clone: pip install -e /path/to/MOVA

# Core dependencies (usually already in ComfyUI)
torch
torchvision
pillow
numpy
tqdm

# MOVA-specific dependencies
accelerate
diffusers
transformers<5
mmengine
yunchang
ftfy
descript-audiotools
einops
imageio
imageio-ffmpeg
